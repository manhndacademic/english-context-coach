# Concept-level mistake patterns

MistakePatterns represent underlying learner misunderstandings rather than exact repeated phrases. This makes review memory more useful for Vietnamese learners because related UserErrors such as literal translations of "push this back" and "move the meeting back" should reinforce the same recurring weakness, even though phrase-specific aggregation would be simpler to implement.
